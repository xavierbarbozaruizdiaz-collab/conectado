
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Las imágenes de productos son públicas para lectura
    match /products/{allPaths=**} {
      allow read: if true;
      // Solo los usuarios autenticados pueden subir/escribir archivos
      allow write: if request.auth != null;
    }
  }
}
